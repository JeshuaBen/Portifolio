import React from "react";

import { Container, Button, Icon } from "./style";

export const Header: React.FC = () => {
  return (
    <Container>
      <Button>
        <Icon></Icon>
      </Button>
    </Container>
  );
};
